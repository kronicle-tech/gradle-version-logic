plugins {
    id "groovy"
    id "java-library"
}

dependencies {
    implementation "org.codehaus.groovy:groovy-all:$groovyVersion"
}

testing {
    suites {
        test {
            useSpock(spockVersion)
        }
    }
}
